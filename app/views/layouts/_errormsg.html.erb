<!--必要変数-->
<!--obj: createでINSERTを行った変数-->

<!--エラーメッセージ-->
<div>
  <% if obj.errors.any? %>
    <h3>
      <% if obj.errors.count== 1 %>
        <%= obj.errors.count %> error prohibited this obj from being saved:
      <% else %>
        <%= obj.errors.count %> errors prohibited this obj from being saved:
      <% end %>
    </h3>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
</div>